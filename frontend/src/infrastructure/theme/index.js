import { colors } from './colors';
import { fontSizes, fontWeights, fonts } from './fonts';
import { sizes } from './sizes';
import { flex, lineHeights, space } from './spacing';

export const theme = {
	colors,
	space,
	lineHeights,
	sizes,
	fonts,
	fontSizes,
	fontWeights,
	flex,
};
